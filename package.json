{
  "name": "miku-confluence-skills",
  "version": "0.4.0",
  "private": true,
  "description": "Agent Skill package for miku-confluence.",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test --test-concurrency=1 tests/*.test.mjs",
    "build": "npm test && npm run build:bundle:zip",
    "build:bundle": "node scripts/build-skill-bundle.mjs",
    "build:bundle:zip": "node scripts/build-skill-bundle-zip.mjs",
    "smoke:runtime": "node --test tests/runtime-smoke.test.mjs"
  }
}
